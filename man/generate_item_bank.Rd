% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CAT_functions.R
\name{generate_item_bank}
\alias{generate_item_bank}
\title{Generate Item Bank}
\usage{
generate_item_bank(
  n_items,
  model = "3pl",
  a_params = c(0.1, 0.2),
  b_params = c(0, 1.5),
  c_params = c(0, 0.3),
  seed = NULL
)
}
\arguments{
\item{n_items}{Integer. The number of items to generate.}

\item{model}{Character. The IRT model to use. Options are "Rasch", "1pl", "2pl", and "3pl". Default is "3pl".}

\item{a_params}{Numeric vector. The parameters for generating the item discrimination (a) values. Default is \code{c(0.1, 0.2)}.}

\item{b_params}{Numeric vector. The parameters for generating the item difficulty (b) values. Default is \code{c(0, 1.5)}.}

\item{c_params}{Numeric vector. The parameters for generating the item guessing (c) values. Default is \code{c(0, 0.3)}.}

\item{seed}{Integer. The random seed for reproducibility. Default is NULL.}
}
\value{
A data frame containing the generated item bank with columns: `item_id`, `a`, `b`, `c`.
}
\description{
This function generates an item bank with specified parameters for different IRT models.
}
\details{
The function generates an item bank based on the specified IRT model. The item parameters are generated using specified distributions:
\describe{
  \item{Rasch}{a = 1, b ~ N(mean, sd), c = 0}
  \item{1pl}{a = log-normal(meanlog, sdlog), b ~ N(mean, sd), c = 0}
  \item{2pl}{a ~ log-normal(meanlog, sdlog), b ~ N(mean, sd), c = 0}
  \item{3pl}{a ~ log-normal(meanlog, sdlog), b ~ N(mean, sd), c ~ uniform(min, max)}
}
}
\examples{
# Example usage
item_bank <- generate_item_bank(n_items = 10, model = "3pl")
print(item_bank)

}
